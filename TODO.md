# 产品体验/差异化待办清单

来源：自查（实测原生窗口）+ 竞品 issue 挖掘（anywhere-labs、dataelement、steven-kid、hairyf、ChisaAlter 等同类桌面壳项目）。
按优先级分组，`[x]` 已完成并提交，`[ ]` 待处理，`[~]` 已调查并决定不做/暂缓（附理由）。

## P0 — 阻断/首因，发布前必须处理

- [x] 右键后退导致启动页卡死，前进键完全无法恢复（真死锁，已实测确认） — `ba8ffef`
- [x] 原生菜单栏与系统标题栏重复，观感陈旧（Windows/Linux 已移除，仅 macOS 保留） — `b6161e2`

## P0.5 — 打包环境专属地雷

- [x] 打包后子进程可能丢失完整 PATH（已从注册表重建 PATH） — `a5ef27d`
- [x] 目录选择器打包环境稳定性（验证结论：桌面壳本身不实现目录选择器，不适用） — 无需改码
- [x] 打包产物运行时依赖完整性冒烟测试（CI 新增 Windows 版 prepare-runtime 冒烟测试） — `07d8d2d`

## P1 — 常规体验核实

- [x] 字体缩放无上限，且无法精确恢复默认值 — `ace2857`
- [x] 生产环境下 DevTools 是否可达 — `ace2857`
- [x] 窗口最小尺寸（960x640）下 UI 有无错位/溢出 — 验证结论：启动页 CSS 自适应正常；harness UI 布局是上游代码，不在本仓库范围
- [ ] macOS 窗口拖动 / Linux 系统标题栏按钮是否正常（需要 mac/Linux 环境，条件受限）
- [ ] 剪贴板写入在当前 WebView2 权限模型下是否受影响（代码走查：无类似 Electron hardenSession() 的拦截逻辑，大概率没问题，未实测点击确认）
- [ ] 系统托盘左右键交互是否符合预期（代码走查逻辑正确，未实测点击）
- [ ] 关闭窗口后托盘通知与图标状态是否正常（代码走查逻辑正确，未实测）
- [ ] 重复启动是否正确聚焦已有窗口而非开新实例（代码走查逻辑正确，未实测"双击图标二次启动"这个真实路径）
- [ ] 窗口拖动/最大化/还原是否流畅（纯交互体验问题，无法通过读代码判断）
- [ ] 深色/浅色系统主题是否跟随 —— 发现真实缺口：启动页 CSS 没有 `prefers-color-scheme` 深色变体；原生标题栏本身跟随系统。要不要做、配色方案需要设计判断，未擅自改

> 以上 6 项均受限于 computer-use 访问被拒，无法在会话内实测收尾；需要重新授权或人工测试后回填结论。

## P2 — 低成本高可见的差异化功能

- [x] 开机自启动选项（托盘菜单可勾选开关） — `6813e90`
- [x] 全局快捷键唤起窗口（Ctrl+Alt+D） — `6813e90`
- [x] 托盘图标状态指示（scoped 为 tooltip 文字：空闲/启动中/运行中/已停止/出错；没有可用的多状态图标美术资源，未硬做图标变色） — `95b6a1b`
- [~] Provider/渠道配置快速导入 —— **调查后判定当前不可行**：需要知道 dsh 自己的配置文件格式，而配置里大概率含 API key，不该在不了解 schema 的情况下猜测性地读写；需要先拿到 dsh 配置格式的确切文档/源码
- [~] 长任务完成后原生系统通知 —— **调查后判定与现有安全边界冲突**：harness 页面被刻意设计为零 Tauri IPC 权限（详见 lib.rs 顶部安全模型注释），没有机制让它告诉壳层"任务完成了"；壳层只能感知服务进程本身的生死，感知不到会话内单个任务的完成状态。要做这个功能，要么破坏现有的 IPC 隔离边界，要么等 dsh 自己在 HTTP 层暴露状态查询接口——都不是这次能单方面决定的事

## P3 — 架构决策（已拍板）

- [~] **是否接入/预集成 DSH 社区插件生态 —— 决定不做。** 理由：（1）把未经审查的第三方代码引入一个本身具备文件系统/shell 执行权限的 agent 工具，是实打实的安全面扩大；（2）目前没有任何同类竞品真正做成过这件事（anywhere-labs 自己都还标注"即将推出"），没有可参照的安全落地范式；（3）预集成意味着把许可证合规和维护责任也背下来。这是有依据的产品决定，不是拖延——如果以后要重新评估，需要先有具体的插件审核机制设计，而不是简单地"接个市场"

## P4 — 大功能投入（超出本轮范围，建议单独立项）

- [ ] 右侧文件预览面板 + Git 分支/工作树可视化 —— 既然不走"集成第三方插件"路线，剩下路径是完全自己原生实现；但这实质上是把整个应用从"单一 webview 指向远程 URL"重构成"自己的多面板原生壳 + 嵌入 harness"，工作量是本轮全部工作的数倍，需要单独的架构设计和排期，不适合仓促做
- [ ] Windows 资源管理器右键菜单集成"用 DeepSeek Harness 打开" —— 需要修改 NSIS 安装脚本、写注册表并保证卸载时干净清理，风险面在安装器层（弄错影响所有未来发布），且需要真实的装卸测试验证，当前没有条件做

---

**本轮完成：11/25**（全部编译通过 + 单元测试通过 + 已提交）。5 项调查后判定当前不可行/需要更大范围规划（已附理由，不是遗漏）。6 项因缺少 live GUI 访问权限停在"代码走查通过，未实测"。
